{
  "name": "@realmlabs/scon-typescript",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.base.json",
    "test": "pnpm build && node --test \"packages/*/test/*.test.js\"",
    "typecheck": "tsc -p tsconfig.base.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.9.0",
    "zod": "^4.1.0"
  }
}
